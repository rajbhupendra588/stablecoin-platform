# Architecture

Milestone 1 establishes independently deployable Spring Boot services, a shared kernel, CI, local Docker dependencies, and consistent security, logging, database, and API conventions.
